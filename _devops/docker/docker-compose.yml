version: '3.3'

services:

  db:
    image: postgres
    restart: always
    ports:
        - 5432:5432
    environment:
      POSTGRES_USER: demouser
      POSTGRES_PASSWORD: pwd123
      POSTGRES_DB: demoapi